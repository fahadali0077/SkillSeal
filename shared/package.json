{
  "name": "@SkillSeal/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for SkillSeal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo 'No lint configured for shared'",
    "test": "echo 'No tests for shared'"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
