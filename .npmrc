# Resolve peer dependency conflicts the npm v6 way — prevents ERESOLVE errors
# from mismatched transitive peer deps in workspace monorepos
legacy-peer-deps=true

# Don't fail the build if optional deps (like fsevents on Linux) are unavailable
optional=true

# Keep install output clean on CI/Render
loglevel=warn
